# FAWE Folia Port — Plan & Status Board

Source of truth for orchestration state. Updated at every transition.

## Phase status

| Phase | Status |
|---|---|
| 0 — Spec | **CLOSED — spec v3 FROZEN** (user-signed; Codex CO-SIGN v3 after r1: 11 findings, r2: 3 residuals, r3: verified). Reviewer thread `019f6ff7-0ac5-7230-aa0c-6aa173b766ad` |
| 1 — Architecture / contracts / decomposition | **CLOSED — [W0-FREEZE] SEALED 2026-07-17**: architecture v3.1 co-signed unconditional; certification r2 = **CERTIFY-WITH-CONDITIONS** (`codex-w0-certification-r2.md`; all conditions applied same day — record sync A2.1/A2.3, count precision); Amendments 1+2 user-signed + Codex co-signed clause-by-clause; inventory frozen (93 items, 5 settled/88 UNKNOWN baseline); numeric budgets → W1-exit gate (A2.3) |
| 2 — Wave 1 (core seams) | **DISPATCHING** (tasks/10-17 approved at the 2026-07-17 gate) |
| 3+ — Waves 2-5 | not started (wave-2 gated on the A2.3 numeric freeze) |

## In-flight work

| Item | Executor | State | Tracking |
|---|---|---|---|
| W0.1 harness bring-up | Codex | **VERIFIED** — boot green, corrective 1 (region-safe smoke via FAWEHarnessProbe) green: exit 0, zero violations. Bot-layer gap closed by pre-staging `ssb2-bots-26.1.jar` (orchestrator-built) | task 01 |
| W0.3 spike lighting | Opus agent | **done → to review** (fillLightNibble GO, skip-light GO, GET reads GO-with-fix, NMSRelighter=DEGRADE target §1b-clean, Starlight STOP-as-structured; 5 runtime Qs → W0.2) | `spikes/w03-lighting.md` |
| W0.4 spike regen | Opus agent | **done → to review** (disposition: DISABLE, source-verified against Folia patches — world creation banned, chunk pump region-owned; one deferred DEGRADE option behind runtime Q1; contract-changing → spec amendment at W0-exit) | `spikes/w04-regen.md` |
| W0.5 spike physics/packets | Opus agent | **done → to review** (GO both mechanisms; physicsFreeze orphaned dead code; send-lock subsumed by commit model; 2 cross-region hazards → W0.2 experiments) | `spikes/w05-physics-packets.md` |
| W0.6 API context audit | Codex (read-only, xhigh, fresh thread) | **done → to review** (382 declarations: 374 PRESERVED / 8 DEGRADED / 0 EXCLUDE; 10 attack points; declared gaps to close at freeze: QueueHandler's 10 public sync/async methods, 24 concrete Extent types' own publics, 4 Fawe UUID-executor methods, stale INV anchor `FaweBukkit.java:475-521`) | `spikes/w06-api-context-audit.md` |
| W0.7 compat inventory | Opus agent | **done → to review** (92 items, 6 axes, 8 cert hooks; attack points declared) | `spikes/w07-compat-inventory.md` |
| W0.9 build plumbing | Sonnet agent | **VERIFIED** (compileJava green, exit 0, pre-existing warnings only; note: scoped gradle invocations need `--no-configure-on-demand`. Pending runtime confirmation via harness: Folia version string + fail-closed path. Corrective 1: folia-api dep removed from bukkit module — JVM25+ metadata; module decision → W0.10) | task 09 |
| W0.2 spike chunk pipeline | Codex impl thread `019f700b` | **DONE — GO recorded** (results doc verified on disk: 323 lines, matrix + ledger + ranking region-sweep>batched>per-chunk + conditional GO with mandatory callback re-dispatch rule = amendment A1.4). Earlier note: consolidation via Opus  (Codex thread cut twice by content-filter false positive on this thread's vocabulary — routing deviation recorded; Codex impl thread reserved for wave-1 code, W0.10a tournament re-examines ranking cross-family). RUN 1 GREEN on Folia 26.1.2, zero violations, restart readback 4096/4096. Key data: prepare/commit model §1b-safe AND fast (commit tasks 364-705µs for 4-16k blocks; visibility ≈ 1 tick scheduler delay); tournament: region-sweep best (4x fewer tasks, equal visibility); ENV: getMinecraftVersion()=="26.1.2" exactly (W0.9 guard correct), PaperLib.isPaper()==true on Folia; scoped neighbor suppression works (0 vs 94 events); relight submits legal from region threads BUT completion callbacks land on non-owner threads (re-hop required in commit contract); SCRATCH loaded=false → //regen DISABLE definitive (W0.4 Q1 closed) | `.codex/w02-iter2.jsonl` |
| W0.8 perf budgets | Opus agent | **done → to review** (structurally freeze-ready; base revision pinned `f53400f00`; ~20 threshold slots + G1-G8 harness perf extensions escalated as hard W0.2 dependency — no §8 number producible until the FAWE driver + timing machinery exist) | `spikes/w08-perf-budgets.md` + 8 scenario specs |
| W0.10b arch co-sign r1 | Codex (fresh, xhigh) | **REJECT — 10 BLOCKING + 1 MAJOR, all accepted by orchestrator** (key: ChunkVersion misses external mutation → base fingerprints required; ticket lexical-extent rule; core/platform placement inversion; QueueHandler binary compat; module decision = dedicated `:worldedit-bukkit:folia` Java-25) | `codex-arch-cosign-r1.md` |
| Architecture v3 revision | Opus (synthesis agent) | **DONE** — v3 on disk, 11 findings applied, judgment rev2, module decision closed | architecture.md v3 |
| APIC gap closure (F9) | Codex (fresh, high) | **DONE** — 271 rows (10+257+4), all PRESERVED; §4c total = 653 declarations | `spikes/w06b-apic-gap-closure.md` |
| G1-G8 harness perf extensions (F10) | Codex impl thread | **VERIFIED** — corrective 3 closed green (actor determinism via `allow-flight=true` + kick wording promoted to fatal scan pattern w/ self-test; Codex thread cut mid-turn, fix verified on disk by orchestrator). Rerun 2026-07-17 17:07: pipeline-probe exit 0, two logs, scan OK both phases, readback 4096/4096. perf-probe green 17:08: full report (region-sweep 65 536 blocks ≈ 1.10 M blocks/s, commit task max 3.3 ms, GC p99 24 ms, queue highwater 4→0). Probe-fillable reference numbers recorded in `w08-perf-budgets.md` §6b; FAWE-driver slots stay wave-1 | `.codex/harness-corrective-3.jsonl`, logs `folia-26.1.2-20260717-1706*/170828` |
| Arch co-sign r2+r3 | Codex (thread `019f7077`) | **CO-SIGN v3.1** (r2: 4 residuals applied verbatim in v3.1; r3: all PASS). Conditional ONLY on the F1 module-graph compile proof to be attached to the freeze record. Harness impl + numeric budgets = wave-1 dispatch gates, not co-sign gates | `codex-arch-cosign-r2.md`, `codex-arch-cosign-r3.md` |
| Compile-proof skeleton | Opus agent | **VERIFIED — GRAPH COMPILE GREEN (exit 0, first pass)**: core SPI + `:worldedit-bukkit:folia` Java-25 module. FD-1 adjudicated: CommitOutcome struck from scope (orchestrator task-writing error; frozen §3.6 ChunkTerminalRecord protocol is the design). F1 condition SATISFIED → arch co-sign now unconditional | compile log `/tmp/graph-compile.log` |
| W0.10 W0-exit freeze + certification | orchestrator + Codex | **CLOSED — r2 CERTIFY-WITH-CONDITIONS, conditions applied** (see phase table). History: r1 REJECT (6 BLOCKING / 1 MAJOR / 1 MINOR; Amendment 1 REJECT-AS-WRITTEN — A1.2–A1.4 supported, A1.1 wording overstates). Correctives dispatched same day: **C-F1F2** (Codex, workspace-write): SPI → v3.1 planSequence/pre-admission registration/NOT_ACCEPTED path + DiagnosticSnapshot on broker; foliaBackend shading config (Paper shadowJar only, reobf clean); FoliaSupport deduped (bukkit main owns detection) — graph compile re-verified green by orchestrator. **C-F3F7** (Opus agent): inventory 92→93 + 5 settled flow-backs (88 UNKNOWN remain), dev records 01/06/09 backfilled evidence-cited. **F8** stale root `spikes/` duplicate archived out of repo. **Packaging proof DONE** (dynamic): Paper jar = 18 Folia-package class files (16 backend-module + detector pair) + 44 core SPI class files; Spigot reobf jar = detector pair ONLY, zero backend classes; single FoliaSupport. C-F1F2 transcription re-verified verbatim by orchestrator against architecture §3.6/G4. Build-env note: old 1.21.x adapters need a local JDK-21 init-script (codebook 1.0.14 vs Java-25 classes; pre-existing, recorded in task 09). Remaining: amendment-2 signature (user) → re-certification round (co-signs A1+A2) → close + commits | `codex-w0-certification.md`, `.codex/c-w0cert-skeleton.*` |

Done earlier: recon reports (3) in `spikes/`; spec co-signature threads r1-r3 (`codex-spec-review-r*.md`), reviewer thread `019f6ff7-0ac5-7230-aa0c-6aa173b766ad`.
Note: **Folia targets RESOLVED by Amendment A2.1 (user-signed + co-signed 2026-07-17)**: 26.1.2 sole certified target (26.1.1 never published — three-source proof in `amendment-2-draft.md`). A2.1 preparation clause operationalized: (1) the orchestrator checks Fill for a published Folia 26.2 stable AT EVERY WAVE GATE (`curl -s https://fill.papermc.io/v3/projects/folia | jq .versions` — currently 26.1 family only); (2) `adapter-26.2` stays in the build matrix; (3) wave-5 certification reserves a 26.2 slot; (4) 26.1.2-specific assumptions must be flagged in dev records; (5) certifying 26.2 requires a new signed amendment.

## Assurance plan (armed modules — spec §6)

All modules armed (assurance=max). Named risk: silent chunk/world corruption + deadlock.
1. Codex spec co-signature — adversarial rounds until convergence (round 1 running).
2. Cross-review (fresh Codex thread) on every delivery touching queue pipeline / adapters / scheduler SPI / history persistence.
3. Double-reviewer on critical domains: concurrency & thread-ownership, chunk write path, data persistence.
4. Runtime harness (adapted from SS2 harness) = wave-0 deliverable; gates scripted and committed.
5. Go/no-go spikes in wave 0: regionized chunk write path, lighting, //regen, API thread-context semantics.
6. Wave-closure certification reviews on waves with intersecting perimeters.
7. Multi-session REPRISE/handoff machinery.
8. Codex CLI workers for HEAVY/XL tasks (persistent implementation thread, ids recorded here).

## Model routing (user-directed, 2026-07-17)

- **Codex CLI (majority)**: development/implementation tasks — persistent implementation thread for continuity; fresh threads for reviews.
  - **REASONING EFFORT = `high` (user directive, 2026-07-20).** All Codex dispatches from this
    point use `-c model_reasoning_effort="high"`; **`xhigh` is superseded** (it was the wave-0 and
    early-wave-1 default, so earlier records citing xhigh are historical, not a template).
    **Never pass `-c service_tier="fast"`.** Workers already in flight at xhigh when the directive
    landed were not restarted — the directive is forward-looking.
- **Opus**: the rest of development, hard design, tricky debugging, adversarial reviews (other family vs Codex).
- **Sonnet**: only very simple tasks (mechanical sweeps, boilerplate, config).
- Orchestrator stays on the strongest model; cross-review keeps implementer and reviewer in different model families.

## Decision log

- 2026-07-17 Fork renamed: `falistos/FastAsyncWorldEdit` (old 2022 repo preserved as `-legacy`). Local: `/Users/falistos/Workspace/forks/FastAsyncWorldEdit`.
- 2026-07-17 Strategy: dual-platform in-tree single jar. REJECTED: divergent Folia-only fork (loses upstream fixes + Paper test surface).
- 2026-07-17 Targets: Folia 26.1.1 + 26.1.2 only. AMENDED pre-signature: Canvas 26.2 dropped by user (Folia fork, no extra API expected; revisit on concrete need).
- 2026-07-17 API guarantee for third-party plugins: YES. REJECTED: player-commands-first.
- 2026-07-17 Degradation policy: documented degradation. REJECTED: strict all-or-nothing.
- 2026-07-17 Assurance: MAX, all modules armed. REJECTED: intermediate (threading bugs invisible to static review).
- 2026-07-17 SS2 integration scenarios: OUT of scope (FAWE standalone).
- 2026-07-17 Spec amended §4b (user-directed): priority order = correctness of world operations first, performance second-but-critical with explicit budgets vs Paper baseline; regionization exploited as a parallelism opportunity.
- 2026-07-17 Routing (user-directed): Codex majority for development + Opus; Sonnet only for very simple tasks.
- 2026-07-17 (user-directed): "best possible architecture — ultra performant AND perfectly safe". Armed: chunk-pipeline design tournament (W0.10a) + architecture.md adversarial co-signature (W0.10b). W0.2 spike must measure competing commit strategies (per-chunk / batched-neighbor / region-sweep), not validate a single design.

## Commit plan (user-approved 2026-07-17, execute on re-certification CERTIFY)

Conventional Commits, no attribution footers, no production-process context in messages.
1. `chore: ignore harness runtime artifacts and session logs` — .gitignore additions
   (`harness/.cache/`, `harness/servers/`, `harness/lib/*.jar`, `.codex/`).
2. `test(harness): add Folia runtime harness with scenario and perf gates` — harness/
   scripts, scenarios, probe sources, lib scripts, versions.env, adapter-jdk21 init script.
   Logs stay untracked per `harness/.gitignore` (runtime captures, regenerable; certification
   reports quote the key lines inline). NOTE: upstream root `.gitignore` ignores `*.sh` —
   `!harness/**/*.sh` re-includes the harness scripts.
3. `build: add dedicated Java 25 folia module` — settings.gradle.kts include,
   gradle/libs.versions.toml folia-api pin, worldedit-bukkit/folia/build.gradle.kts.
4. `feat(core): add Folia port task SPI skeleton` — worldedit-core util/task/* (19 files) +
   QueueHandler visibility change.
5. `feat(bukkit): add Folia detection with fail-closed certified matrix` — FoliaSupport,
   UnsupportedFoliaVersionException, WorldEditPlugin onLoad block, plugin.yml.
6. `feat(bukkit): add Folia backend service skeletons` — worldedit-bukkit/folia/src classes.
7. `build(bukkit): bundle the folia backend into the Paper artifact only` —
   worldedit-bukkit/build.gradle.kts foliaBackend configuration.
8. `docs: add Folia port specification, architecture and wave-0 records` —
   .orchestrate/folia-port/ (spec, amendments, architecture, plan, spikes, tasks,
   certification reports).

## Future triggers

- Architecture v3.1 CO-SIGNED + compile proof green → freeze record ready; wave-0 certification review next (fresh reviewer, cross-seam sampling, includes explicit Amendment-1 co-signature).
- Amendment 1 USER-SIGNED 2026-07-17 (regen DISABLE, relight DEGRADE, tick-limiter DISABLE, callback re-dispatch rule). Codex co-sign pending in arch round `codex-arch-cosign-r1`.

- W0-exit spec amendment round (single batch, user signature + Codex): //regen DISABLE (W0.4), relight DEGRADE to NMSRelighter + Starlight STOP-as-structured (W0.3), tick-limiter disable on Folia (W0.5), any further spike outcomes. Never piecemeal signatures.
- W0.2 probe build gotcha: ChunkPos.x/z are record accessors x()/z() in 26.1 mappings (orchestrator fixed). Codex turn-1 was killed by a content filter (false positive) at the last file write — use neutral measurement vocabulary in Codex prompts on this thread.
- FAWE-jar in-matrix boot leg deliberately skipped: out-of-matrix abort is deterministic code → wave-3 deterministic tests own it.
- Next W0.2 iteration prompt must include: implement the perf-extension contract from `harness/scenarios/perf-index.md` (G1-G8 — @mark, samplers, perf-report, FoliaRegionDispatcher commit-timer hook in the probe) so §8 numbers become producible.
- W0.10 architecture input: dedicated Gradle module for the Folia backend (Java 25 toolchain, SS2 `Hooks/Folia` precedent) vs in-module — forced by folia-api JVM25+ metadata; decide at architecture co-sign.

- When Codex spec review r1 lands → adjudicate findings; if amendments: amend spec + re-sign (user) + Codex round 2. Repeat until CO-SIGN. NOTE: r1 started against the pre-§4b spec (amendment landed mid-run) — explicitly surface §4b (priority order) to Codex in the next round.
- When EditSession recon lands → write `spikes/recon-editsession-paths.md`, then draft `architecture.md` (needs: spec co-signed OR at least round-1 findings integrated).
- Phase 1 gate: present wave plan + frozen contracts + assurance plan to user before any dispatch.

## Verified facts (for architecture)

- folia-api artifact used by the SS2 prod stack: `dev.folia:folia-api:26.1.2.build.8-stable` (compileOnly), Java 25 toolchain required at runtime for MC 26.x (source: SS2 `Hooks/Folia/build.gradle:18`).

## Waves

Dependency rule: a task starts only when its deps are `verified`. Routing per user
directive (Codex majority + Opus; Sonnet only trivial). Wave 0 closes with a certification
review + the [W0-FREEZE] contract co-signature (exact SPI signatures, §4c disposition
table, inventory, budgets) — nothing in wave 1+ dispatches before that.

### Wave 0 — De-risk & foundations (all tasks independent unless noted)

| ID | Task | Executor | Notes |
|---|---|---|---|
| W0.1 | Harness bring-up: Folia 26.1.1+26.1.2 server automation (adapt SS2 harness), smoke scenario (//set + block verify), watchdog/thread-violation detection | Codex (persistent impl thread) | exclusive: harness/, ports 25601-2 |
| W0.2 | SPIKE go/no-go: regionized chunk write path prototype on adapter-26.1 (detached prepare → region commit → finalizers), perf measurement vs Paper | Codex (impl thread, after W0.1 usable) | THE architectural bet |
| W0.3 | SPIKE: lighting under regionization (starlight relight per-region) | Opus | GO/DEGRADE/DISABLE/STOP |
| W0.4 | SPIKE: //regen feasibility on Folia | Opus | GO/DEGRADE/DISABLE/STOP |
| W0.5 | SPIKE: physics suppression + chunk packet resend without global toggles | Opus | feeds C1 |
| W0.6 | API thread-context audit → §4c disposition table draft (every public entry point) | Codex (fresh thread — audit, not impl) | [W0-FREEZE] input |
| W0.7 | Compatibility inventory draft (coverage baseline per spec §7.2) | Opus | co-signed at W0 exit |
| W0.8 | Perf budget artifact: workloads, metrics, dual baselines (spec §8), Paper baseline measurements | Opus (definition) + harness runs | needs W0.1 |
| W0.9 | Build plumbing: folia-api dep (pinned), FoliaSupport detection, fail-closed bootstrap skeleton, plugin.yml | Sonnet | small, well-specified |
| W0.10a | **JUDGED — synthesis in progress** (3 proposals delivered; consensus: sliced region-sweep. Ruling: skeleton=C RegionTicket/choke-point/typed-sink, engine=B region lanes, grafts=A owner-inline+viewer-near+packet-at-commit config-gated. Synthesis worker rewriting architecture.md v2 + judgment.md) — chunk-pipeline design tournament (user-directed 07-17): ≥3 independent proposals (Codex + Opus, imposed angles: latency-first / multi-region-throughput-first / safety-simplicity-first), judged against W0.2 harness measurements + §1b compliance; winner synthesized (grafting runners-up ideas) into architecture.md | orchestrator judges; mixed families propose | after W0.2 |
| W0.10b | architecture.md adversarial co-signature (same regime as spec: Codex rounds until convergence) + [W0-FREEZE] contracts finalized, co-signed (user + Codex); certification review of wave | orchestrator + Codex | gate for wave 1 |

### Wave 1 — Core seams (after W0.10)

Task files DRAFTED (2026-07-17, `tasks/10-17`, pending gate approval):
10 thread-context resolver + Bukkit backend → 11 completion protocol → 12 ticket
authority/dispatcher → 13 backpressure (Demand) → 14 commit-broker lanes + G4 diagnostics →
15 C5 requalification (~20 isMainThread sites, per-site record) → 16 FoliaTaskManager/
FoliaQueueHandler → 17 bootstrap wiring (closure task, includes A2.1 consequential edit of
the FoliaSupport certified set). Deps: 10 and 11/13 are roots; 17 last. Codex majority;
cross-review armed per delivery; double-reviewer on concurrency/ownership perimeters.
Drafts 11/14 re-synced after corrective C-F1F2 (planSequence + DiagnosticSnapshot now in tree).

**[W1-MILESTONE] GRAPH COMPILE GREEN AGAIN 2026-07-20** (`/tmp/graph-compile-s4b.log`, exit 0,
pre-existing warnings only) after the completion seam moved five times in one session (r10→r15).
The C5 guard executed inside that build on all three projects (`:worldedit-core`,
`:worldedit-bukkit`, `:worldedit-bukkit:folia`) and is `up-to-date` on re-run, confirming the
`outputs.file(stamp)` fix. Task 13's fresh corrective 2 also reports full-Folia-sources compilation.

**REVIEW DEBT — nothing below is sealed, and this is deliberate.** The only adversarial reviews
run this session covered task 11 correctives 1+2, task 13's fresh delivery, task 15's delivery and
task 16's delivery. **Unreviewed corrective work now on disk:** task 11 correctives 3/4/5, task 12
alignments 2/3, task 13 fresh correctives 1/2, task 15 corrective 1, task 16 corrective 1. Given
that EVERY lens run this session found something real — several catastrophic, two of them
compile-clean — none of this may be sealed on worker self-report. Re-reviews are owed before the
W1-exit gate.

Wave-1 execution tracking (updated 2026-07-20, session 4 — resumed from the 07-17 pause):
Session-4 dispatch: (a) task-12 alignment pass running on its own Codex thread
`019f712d-4dec-7001-ba67-604ca088d997` (`.codex/w1-task12-alignment.*`), applying task-11's
corrective-2 migration recipe verbatim + folding the 3 deferred shutdown-race MINORs; (b) task-11
correctives 1+2 re-review running as TWO fresh Opus reviewers (double-reviewer per spec §6),
lenses = persistence/exactly-once and concurrency/ownership, reports →
`review-w1-task11-c1c2-{persistence,concurrency}.md`; (c) task-13 fresh-worker prompt staged at
`.codex/w1-task13-fresh.prompt.md`, dispatch held until the 12 alignment is verified (recorded
integration sequence). Doc sync applied: architecture §3.6b/§3.6c inline headers were stale
(§3.6c still read "PENDING co-signature" though r9 CO-SIGNED it) — corrected against
`.codex/arch-cosign-r9.msg`.


| Task | State |
|---|---|
| 10 thread-context | **VERIFIED** — delivered (ContextResolver CAS + BukkitThreadContext); Opus cross-review PASS-WITH-NOTES (1 MINOR: `isFaweWorker` = extent-carrying prepare workers ONLY — binding note recorded in task file + interface comment) |
| 11 completion protocol | **RE-REVIEW 2026-07-20: BOTH LENSES REJECT** (`review-w1-task11-c1c2-{persistence,concurrency}.md`). **Independent convergence on the two hard defects** — the two reviewers never saw each other's work and landed on the same two lines, which is the strongest signal this regime produces: (i) `SameChunkPlanSequencer:84` un-isolated stage (persistence F5 / concurrency BLOCKING 1 — the concurrency lens escalates it to BLOCKING with a sharper argument: the consumer continuation runs on the control thread INSIDE `serializationLock`, i.e. the exact lock-held-across-`future.complete` shape that got task 13 rejected twice, and post-TERMINATED it lands on a TICK thread); (ii) `closeAdmission` post-TERMINATED stranding the coordinator (persistence F1 / concurrency BLOCKING 2 — §3.6b names this failure verbatim, "no stranded or duplicated COMPLETING", and the code produces it). Both also independently flag the unbounded flush and the test-quality problem: three of the four race-named tests cannot fail on the race they are named for, and no test anywhere can observe a lock held across a completion — which is why BLOCKING 1 survived two correctives. Concurrency lens adds MAJOR 4 (inline fallback unenforced: `closeAdmission` on a tick thread takes a FAIR lock and runs O(chunks) `buildResult`). Both confirm correctives 1+2 genuinely CLOSED their own two MAJORs and rate the isolation machinery itself unfaultable — these are single-point defects, NOT a design failure, so the disposition is a bounded corrective 3, **not** a task-13-style re-plan. **Adjudication: 4 of the findings are contract questions, not implementation choices** → architecture ruling round **r10 DISPATCHED** (`.codex/arch-cosign-r10.*`, resumed co-signer thread `019f7077`): Q1 drain deadline unenforceable by any caller, Q2 producer fence not total between preflight and registration, Q3 aggregate rejection cause has no channel (possible frozen-SPI signature change → would route to the USER gate, not to a worker), Q4 inline concession vs §1b on tick threads. **r10 RULED (`.codex/arch-cosign-r10.msg`) and dispositioned 2026-07-20:** Q1 → ruling (b), new §3.6b "Drain-expiry terminalizers" paragraph (bounded idempotent flush-expiry hook registered atomically with the producer; `flush` itself deadline-bounded); Q2 → a live `AdmissionLease` carries exactly one producer-registration capability, `registerProducer(lease, planKey, flushExpiryHook)` accepted through `FLUSHING`, so lifecycle transition alone can no longer reject registration after a successful preflight; Q4 → post-termination inline fallback restricted to non-tick asynchronous callbacks with bounded internal work, tick callers fail before acquiring the primitive. All three = clarifications within co-signed intent, no frozen SPI change → **APPLIED DIRECTLY to architecture.md §3.6b/§3.6c** by the orchestrator. **Q3 → FROZEN SPI SIGNATURE CHANGE, USER SIGNATURE REQUIRED** (`closeAdmission()` → `closeAdmission(Optional<Throwable> aggregatePreRegistrationRejection)`), plus the three-way classification rule (zero candidates and no failure → SUCCEEDED no-op; any pre-registration rejection with no applied mutation → FAILED; rejection plus applied mutation → PARTIAL) → drafted as **`amendment-3-draft.md`, PENDING USER SIGNATURE**, deliberately NOT applied and NOT delegated to any worker. **Corrective 3 DISPATCHED** (`.codex/w1-task11-corrective3.*`, resumed worker thread `019f710d`) covering the 2 BLOCKINGs + Q1/Q2/Q4 + the boundary-timeout ordering + the sequencer head-terminal wedge + 4 MINORs + a mandated test-quality pass, with Q3 explicitly EXCLUDED (the worker only neutralizes the test that asserts the forbidden `SUCCEEDED`, leaving a `// pending Amendment 3` marker so the gap stays visible). Bounded-loop rule restated to the worker: round 3 is final; escalate rather than iterate. Corrective 3 must also emit a **task-13 migration recipe** for the Q2 lease/registration change, since task 13 is being written concurrently against the pre-r10 API — same mechanical alignment pattern that worked for task 12. **CORRECTIVE 3 DELIVERED 2026-07-20 — scope points 1-3 and 5-9 done and orchestrator-VERIFIED on disk; point 4 legitimately ESCALATED.** Both BLOCKINGs are genuinely closed: `SameChunkPlanSequencer` now returns `completionService.isolateStage(ready)` instead of a raw `minimalCompletionStage()`, and the stranding fix is sound in shape — `maybeComplete` now passes the terminal-state transition as a CALLBACK into `publishOutcome`, so the state only advances if publication actually succeeds, and rolls back to `OPEN` if it throws, making a later `maybeComplete` able to recover. The named regression tests exist in the right files (`closeAdmissionAfterCompletedFlushDoesNotStrandOrRepublishResult`, `readyContinuationAttachedAfterTerminationIsStillIsolated`, `headCanTerminalBeforeAsynchronousReadyPublication`, `armedPersistenceTimeoutCanExpireWhileBoundaryPrefixBlocks`), i.e. the test-quality mandate was taken seriously; 54 focused tests with assertions enabled. **The escalation is exemplary and I am accepting it:** r11 landed DURING this worker's verification and rescinds its point-4 r10 coupling, and r11 named `PlanProducerToken`/`AdmissionProducer`/`AdmissionDeliveryLease` without signatures — the worker refused to invent a three-task seam and stopped. **IMPORTANT bookkeeping: a task-11 corrective 4 will be needed, and it is NOT a bounded-loop failure** — the contract changed above the worker mid-flight, which is a different category from failing to converge; the loop discipline stands. → **architecture round r13 DISPATCHED** (`.codex/arch-cosign-r13.*`) for the exact signatures and lifecycle of the three collaborators, so ONE ruling unblocks task 11 (corrective 4), task 13 (corrective) and task 14 (dispatch) against a single coherent contract. Amendment 3 explicitly flagged as pending so no signature folds it in prematurely. Detail follows. Correctives 1+2 CLOSED their own two MAJORs (timer-handle retention, finalizer drain clamp — reviewer verified both non-vacuously), but the round surfaced 1 BLOCKING + 6 MAJOR. **Orchestrator-verified on disk (5 of them, independently):** (F1, BLOCKING) `maybeComplete` advances `state` past OPEN and THEN calls `publishOutcome`, which throws when the service is TERMINATED; `closeAdmission` reaches it through `completionService.execute`, which runs inline post-TERMINATED and swallows the throwable into a discarded future → the actor's result is lost permanently and silently, `state()` still reports SUCCEEDED. (F2) `buildResult` classifies an EMPTY registration map as SUCCEEDED and r2 amendment 2's aggregate rejection cause is simply not implemented (`closeAdmission()` is no-arg); worse, `DefaultOperationCompletionTest:700` ASSERTS `SUCCEEDED` for an operation whose only registration was rejected — a green test locking in the successful-empty completion the architecture forbids. (F3) `effectiveDeadlineNanos` has exactly ONE call site, so the drain clamp covers settlement deadlines only: a registered-but-never-terminalized plan fences `flush()` unboundedly, and §3.6's drain-terminalization wording has no implementation and no real test (`drainTerminalizationHasNoIncompleteState` asserts an enum constant, never calls flush). (F5) `SameChunkPlanSequencer:84` returns a raw `minimalCompletionStage()` completed on the control executor — the one delivered surface that escapes corrective 2's isolation, contradicting its dev record; currently LATENT (grep: no consumer yet), so task 12's seal is unaffected. (F6) `registerProducer` is ACCEPTING-only while the lease deliberately survives FLUSHING, so `register` can fail after a successful preflight — §3.6c's "once a plan is registered, its lease is guaranteed" is not total; fix belongs in 11, not 13 (the in-flight 13 worker was already instructed to `deliverExceptionally` on that path, so it is not blocked). Unverified-but-plausible: F4 (boundary invoked on the control thread before its timeout is armed), F7 (sequencer head `terminal()` before its async `ready()` fires wedges the chunk lane). Dev-record inaccuracies recorded: the advertised lease "assert-plus-diagnostic invariant" is unreachable dead code. History: double-review PASS-WITH-NOTES → corrective 1 dispatched — exactly-once core CONFIRMED sound under all traced races; settlement bounds real; status separation holds; CompletionStage surface locked. To close: MAJOR timer-handle retention (uncancelled ScheduledFutures retain the coordinator), MAJOR finalizer boundary lacks the drain clamp persistence got (never-settling finalizer hangs flush — §3.6 drain-wording breach), correctives 1+2 BOTH DONE: c1 = timer-handle cancellation + symmetric finalizer clamp (drain-clamped) + throwing-transition settlement + race tests; c2 = §3.6b/§3.6c ALIGNMENT COMPLETE — isolated per-task notification delivery (virtual threads), `tryAcquireLease` ACCEPTING-only preflight + `ProducerLease` idempotent delivery through FLUSHING, termination fenced by outstanding leases, CompletionServiceStage/Future fully isolated incl. post-termination. Migration recipes RECORDED in the c2 dev record: task-12 alignment (registerProducer + isolateStage wrappers at :288/:516, obsolete FoliaCompletionStage/Future removable) and task-13 fresh-impl usage (inject service, admissionAttemptId, lease-before-register, deliver/deliverExceptionally). Escalation none; terminal re-reviews conform. **Next: 12 alignment pass, then re-review of 11 c1+c2 by its double-reviewer.** History: (adjudication hardened over rounds r4 REJECT → r5 one MAJOR → r6 **CO-SIGN**: settlement with finite bounds, `HistorySettlement` in AppliedReceipt + SPI, ACCEPTING→FLUSHING→TERMINATED completion service w/ producer fence, TerminalStatus = world-mutation only). New W1-exit numeric slot `HISTORY_PERSIST_RETRIES`. Worker completing implementation + tests on its own thread |
| 13 backpressure | **DOUBLE-REVIEW 2026-07-20: BOTH LENSES REJECT** (`review-w1-task13-{liveness,admission}.md`). Independent convergence on `deliverAll` (`:1049-1057`) having **no per-delivery exception isolation**: one throw abandons every later entry in the batch, and those entries are ALREADY settled in accounting — queue-removed, counters decremented — so their permits never release, their `acquire()` stages never complete (the caller hangs silently), their leases never leave `producerLeases`, and `terminateIfQuiescent` can never fire, i.e. **shutdown never completes**. Reachable on the ordinary shutdown path. Other BLOCKINGs: (liveness) the cancellation callback at `:424` is a non-async `whenComplete` on a consumer-supplied stage, so a `//cancel` or chunk unload completing it **on a region thread** runs the whole settlement+delivery cascade there — the same failure family as retired attempt #1, relocated to the cancel path; (liveness) `tryAcquire` **blocks and allocates** — `tryAcquireLease` takes the completion service's GLOBAL `lifecycleLock` and starts a virtual thread on every call including every miss, so the "provably never blocks" acceptance criterion is NOT met; (admission) **continuations bypass byte/chunk accounting entirely** — `grant()` adds nothing to `readyBytes`/`globalReadyBytes`/`readyChunks` for CONTINUATION and `enter(FINALIZING)` releases ready bytes one step BEFORE the continuation is taken, so prepared bytes grow unbounded outside the `min(1 GiB, maxHeap/4)` global cap while `pressure().readyBytes` reports 0. **Seeded question answered decisively, against the temurin-25 JDK sources**: yes, common-pool starvation is real — `delayedExecutor` runs expiry on `ForkJoinPool.commonPool()` (one worker on a 2-vCPU host), backpressure self-starves by submitting its own waiter service to that same pool where it takes a blocking lock with no `ManagedBlocker` compensation, the pool is shared with every other plugin, and those tasks sit OUTSIDE `flush`'s lifecycle fence. **ORCHESTRATOR ERROR OWNED:** my salvage ruling ("accounting/carve/permit/eviction proven in correctives 1-2") was NOT fully safe — the earlier reviews were scoped to the completion seam, so that machinery entered this attempt with near-zero adversarial coverage; B2 + 2 MAJORs + 2 MINORs are salvage-imported, and only the `deliverAll` defect came from the §3.6c rebuild. What the salvage genuinely got right (reviewer could not fault): `transfer` atomicity, the `pruneIfIdle` predicate, the stage machine, `close()` idempotence, no double-release across seven paths, and a truly subtractive per-region carve. **The planned r10 alignment is NOT mechanical** — under r10 Q2 `queueDelivery` hard-throws unless `registerProducer(lease, planKey, hook)` ran, which backpressure never calls, so every delivery would throw; and a lease-per-`tryAcquire` becomes a producer-registration per owner-thread miss, which is untenable. → **architecture round r11 DISPATCHED** (`.codex/arch-cosign-r11.*`) on the four contract questions: lazy vs eager lease for the nonblocking owner-thread entry, flush-expiry hook for admission waiters (r10 Q1 requires one and §3.6c gives them none), the deadline-expiry mechanism, and where a consumer-supplied cancellation continuation must hop. Task-13 corrective HELD until r11 rules, so the fixes and the re-scoped alignment land in ONE round. `RegionKey` promoted from reconciliation note to **HARD PRECONDITION on task 14** (recorded in its task file): identity equality + a per-drain mint would silently degenerate every per-region cap into a per-attempt cap — the gate failing open invisibly. History: fresh implementation delivered 2026-07-20 (§3.6c) — orchestrator pre-verification (two fresh Opus lenses: liveness/§1b and admission-correctness → `review-w1-task13-{liveness,admission}.md`). Orchestrator pre-verification on disk, BEFORE review: the three failure modes that killed rounds 1–2 are structurally gone — grep finds NO private thread, executor, drainer, `completionLock` or tick-detection anywhere in the file; the only lock left is `accountingLock`, which is exactly §3.6c's state-settlement primitive; `tryAcquireLease` is called before any accounting (`:373` precedes `accountingLock.lock()` at `:387`); and **every** `deliverAll(...)` call site follows an `accountingLock.unlock()`, including inside `expire()`'s `finally` — i.e. settlement accumulates an `AdmissionDelivery` list under the lock and delivery happens strictly outside it, which is the shape both earlier rounds failed to reach. 1222 lines (vs 1240 pre-existing), salvaged machinery retained. **Seeded question handed to the liveness reviewer:** `:455` schedules deadline expiry through `CompletableFuture.delayedExecutor(...)`, i.e. the JDK common ForkJoinPool — legal under §3.6c (backpressure may own no thread of its own) but the whole re-plan was triggered by "deadline expiry must never be blockable", so common-pool starvation must be ruled on explicitly rather than assumed benign. Worker escalation `NEEDS_CONTEXT` (frozen SPI cannot express lease-before-`register` sequencing to task 14) is **already answered by r10 Q2**, which lands in the planned alignment pass. Reviewers were told NOT to fault the pre-r10 call shape but to flag what r10 would break. History: corrective 2 TERMINAL-RED → RE-PLAN (bounded-loop rule honored; worker's own dual review rejected its design honestly). Root cause adjudicated: settlement/notification conflation. Re-plan = architecture §3.6c (settlement serialized under accounting lock, no user code, expiry = settlement effective immediately; delivery handed to the injected §3.6b completion service — no bespoke thread/fallback in backpressure). **§3.6c CO-SIGNED r9** (r7: 3 BLOCKING+1 MINOR applied; r8: §3.6b mirror edit — observable completions on isolated notification tasks — + lease preflight BEFORE register; r9 clean). Integration sequence before the fresh 13 worker: (1) 11-corrective-1 lands → (2) 11-corrective-2: notification-task isolation (r8 am.1) + nonblocking producer-lease preflight API + admissionAttemptId delivery support → (3) 12 alignment pass (registerProducer drift + its 3 shutdown-race minors) → (4) FRESH 13 worker against the stabilized core API (salvage: accounting/carve/permit/eviction machinery proven in correctives 1-2) → (5) graph compile → 14. History: corrective 2 dispatched — corrective 1 fixed all 7 original findings without regression, but its replacement fallback introduced: NEW BLOCKING (inline rejection drain can run on a TICK thread and block unboundedly on completionLock — §1b breach; §3.6b's inline concession covers async producers only) + NEW MAJOR (completionLock held across future.complete → user continuations in critical section; deadline expiry gated behind the lock). Reviewer prescribed exact shapes: tick submitters enqueue-and-return; plain UNMARKED lifecycle-managed fallback drainer (B1's defect was the marking, not the thread); batch-drain outside the lock with single-drainer guard. Red after round 2 ⇒ re-plan. Task-14 wiring note recorded (rejection hook must not block in the drain path) |
| 12 ticket authority/dispatcher | **ALIGNMENT VERIFIED 2026-07-20 (orchestrator, on disk) — graph compile GREEN** (exit 0; `/tmp/graph-compile-s4.log`; the `registerProducer` drift is closed). Recipe applied verbatim and independently re-verified: `AtomicReference` → failure-aware `registerProducer` → waiter construction → reference publication → lifecycle transitions (`:257-294`); `failDrain` terminal-CASes once, removes the waiter, cancels the deadline task, completes only the PRIVATE raw future exceptionally (`:524-541`); both public views go through `completionService.isolateStage` (`:294`, `:543-545`); accounting settles BEFORE publication (`:460-468`); `FoliaCompletionStage`/`FoliaCompletableFuture` deleted with zero residual references tree-wide. 3 deferred MINORs disposed: (1) post-TERMINATED inline tick-side → all completion sites route through `submitCompletion`, which hops a tick caller onto an isolated notification before touching the service; (2) `requestFinishDrain` producer-state guard → added, rejected `Producer.complete` routed to `failDrain`; (3) 1-tick entity latency → NOT a defect: `EntityScheduler.execute` javadoc states "Any value less-than 1 is treated as 1" (orchestrator-verified against the folia-api sources; 4-arg signature confirmed on the 26.1.2 target jar) — retained as a documented platform constraint. **PERF ATTACK POINT for the W1-exit numeric freeze (A2.3):** `submitCompletion` now costs one isolated notification task per tick-origin completion on the hottest scheduler seam, paid in steady state to close a shutdown-only race. Correctness-preserving and within the worker's no-core-edit constraint, but it should be MEASURED at the perf probe; a core-side never-inline submit variant would remove the steady-state cost if the number justifies it. Ordering is not at risk (transitions are per-submission and independent; `settle` reaching zero is order-insensitive). History: cross-review PASS-WITH-NOTES — ticket soundness/choke point/A1.4 re-dispatch/drain/confinement ALL CLEARED (no escape constructible; mint re-checks ownership; 5 completion sites verified). 1 MAJOR integration: task-11 corrective changed `registerProducer` signature → `DefaultFoliaRegionDispatcher.java:255` stale, folia compile red until reconciled (orchestrator-sequenced, not a 12 design fault). 3 MINOR shutdown races (post-TERMINATED inline can land tick-side; requestFinishDrain lacks producer-state guard; 1-tick entity latency) → fold into the 12 alignment pass |
| 15 C5 requalification | **DOUBLE-REVIEW 2026-07-20: BOTH LENSES REJECT.** The Paper-regression lens — added on orchestrator initiative, NOT in the task plan — paid for itself twice over: it found two BLOCKING defects that are pure Paper-side and **compile-clean**, which is exactly why the worker's `javac`-only verification missed them. (1) **Infinite recursion, orchestrator-CONFIRMED end to end:** task 15 added a `BukkitAdapter.adapt(world)` argument in `PaperweightFaweAdapter.createWorldNativeAccess`; that resolves to `IBukkitAdapter:322` `new BukkitWorld(world)`, whose constructor calls `createWorldNativeAccess(world)` again — no base case, no override in `CachedBukkitAdapter`/`SimpleBukkitAdapter` → `StackOverflowError` on the FIRST world adaptation on any Paper 26.1 server. (2) **No `FaweThreadContext` is ever registered in production** (both lenses, independently): `ContextResolver.register` is called only from two TEST classes, `BukkitThreadContext` is never instantiated, and `resolve()` throws on null — so all 30 requalified sites throw at runtime, on Paper too, and `QueueHandler.run()` (a per-tick task) throws every tick. Registration is task 17's job, but the reviewers surface a design point that survives task 17: `Fawe.isMainThread()` was TOTAL (deliberately `true` pre-boot), the replacement is PARTIAL and throws. (3) **Semantics lens, sharper still — three ownership guards are provably INERT:** task 15 widened the location-free `TaskManager.sync` short-circuit to `isTickThread()` (`TaskManager.java:375`), so the FALSE branch of `ownsEntity` at `LazyBaseEntity:37` and of `ownsCachedChunks()` at `PaperweightFaweWorldNativeAccess:289` funnels straight back into `sync` and runs the payload **inline on the very non-owning region thread the guard excluded** — live NMS entity serialization and foreign-region `setBlockState`/`sendChunk`. On Folia this is **strictly less safe than the naive translation**, since legacy code would at least have enqueued: the pass shipped §3.5's permissive half and deferred its protective half. Also: `NMSAdapter`'s new World overload runs the identical live-section CAS in BOTH branches, so its `ownsChunk` gates nothing; and `BukkitBlockCommandSender:194` guards live chunk access with `Bukkit.isPrimaryThread()` plus a C1-banned `BukkitScheduler` fallback — invisible to the C5 inventory and its guard, which only look for `Fawe.isMainThread()`. MAJORs: a `BukkitWorld` allocated per `ownsChunk` call; a new `WorldUnloadedException` surface where `getNameUnsafe()` was the right call; `FaweBukkitWorld.of(...)` putting a global `synchronized` map on the parallel chunk-get path. Guard hygiene: the checker file is UNTRACKED while `build.gradle.kts` is modified — split those commits and every build in the repo fails; no `outputs` declaration; cross-project configuration contradicts `configureondemand=true`. **What held up under pressure** (reviewers tried and failed to break it): the four adapter ownership rows, `ownsCachedChunks()`'s every-target conjunctive formulation, the `NMSAdapter` overload split across all nine adapter trees, and `QueueHandler.run()` → `isGlobalContext()`. → **architecture round r12 DISPATCHED** (`.codex/arch-cosign-r12.*`) on the two contract questions: what the false branch of an ownership guard must do and which task owns it, and whether `FaweThreadContext` resolution must be TOTAL (with the asymmetry that a permissive pre-registration default is behaviour-preserving on Paper but fail-open on ownership on Folia). **r12 RULED and APPLIED 2026-07-20** (`.codex/arch-cosign-r12.msg`; all three = clarifications, NO frozen SPI change): **Q1** → option (b), new §3.5 paragraph "ownership guards are authorization, not routing" — the false branch MUST route through the target-bearing `syncOn`/ticketed dispatcher and MUST NOT fall back to location-free `sync`; task 15 must WITHHOLD the three inert guards rather than ship them, **task 16 owns the final routes**, and task 15's completion gate now depends on them. **Q2** → the ruling went AGAINST restoring totality: resolution stays **partial and fail-closed** (new §3.1 paragraph), because no permissive default both preserves Paper's legacy result and protects Folia ownership — bootstrap ordering becomes contractual instead, **task 17 owns registration + an ordering test** proving `QueueHandler.run()` cannot precede it. **C5 perimeter widened** to every production predicate authorizing inline execution or an ownership-sensitive fallback (`Bukkit.isPrimaryThread()`, `isSameThread` equivalents, direct thread comparisons), since the guard was enforcing a spelling rather than a semantics; the stale "~20 sites" figure in the C5 block was corrected to ≈32. Added scope recorded in the task 16 and task 17 files. **Task-15 corrective 1 DISPATCHED** (`.codex/w1-task15-corrective1.*`, resumed thread `019f7eb4`) covering the recursion, the withheld guards, the inert `NMSAdapter` gate, the early-call-site audit, the perimeter widening, 4 Paper MAJORs, guard hygiene (untracked checker + modified `build.gradle.kts` = commit-split breakage; no `outputs`; cross-project config vs `configureondemand`) and 8 MINORs — with instruction to RECORD rather than silently fix the two pre-existing unsoundnesses the pass preserved. History: DELIVERED 2026-07-20 (lenses: predicate semantics → `review-w1-task15-semantics.md`; **Paper/Spigot regression** → `review-w1-task15-paper-regression.md`. The second lens was added because nobody had asked it: 30 sites changed in a dual-platform single jar, and a silent Paper regression would hit far more users than any Folia bug). Result: **32 rows — 30 requalified, 2 deliberately kept, 0 escalated**; the worker re-derived the inventory as instructed and corrected the orchestrator's own count (TaskManager had 8 call sites, not 9). Orchestrator verification on disk: only the 2 intended references remain in the Folia-enabled source sets (the `Fawe.java:210` declaration and the `BukkitThreadContext.java:35` backend that legitimately implements it); adapter-26.1 is fully treated; the 14 uncertified adapter files are untouched, per A2.1. Predicates are properly DIFFERENTIATED rather than swapped mechanically — `isGlobalContext()` for global toggles/drain, `isTickThread()` for offload/no-block decisions, and `ownsChunk`/`ownsEntity` on every live-access path. Spot-checked two of the highest-blast-radius sites in code: `SingleThreadQueueExtent:260` gates `chunk.call()` on exact-chunk ownership, and `PaperweightFaweWorldNativeAccess:289`'s `ownsCachedChunks()` is genuinely CONJUNCTIVE over every changed chunk AND every chunk-to-send — not an anchor-chunk approximation. **Build guard independently re-run by the orchestrator** (directly, bypassing Gradle): `NoFaweIsMainThread.java` self-test PASSES (planted violation rejected, exit 1) and the real tree is clean (exit 0); it catches the static-import evasion and its call-shaped regex naturally spares the declaration without blanket-excluding `Fawe.java`. Wired as a `dependsOn` of `compileJava` for core/bukkit/adapter-26.1/folia in `build.gradle.kts` — note this now runs inside every graph compile proof. History: DISPATCHED 2026-07-20 (fresh Codex thread, xhigh, `.codex/w1-task15.*`). Unblocked early: depends only on task 10 (VERIFIED), and the folia module has ZERO `isMainThread` references, so its file set is disjoint from the running 13/16 workers. Inventory correction passed to the worker: the task file's "~20 sites" is STALE — the true count in the Folia-enabled source sets is ≈32 (core ≈24, bukkit main ≈4, adapter-26.1 ≈4). `adapter-26.2`/`adapter-1_21*` deliberately EXCLUDED (not Folia-enabled per A2.1) with instruction to record the future-26.2 obligation rather than expand scope unilaterally |
| 16 task manager / queue handler | **DELIVERED + routing review = PASS-WITH-NOTES** (`review-w1-task16-routing.md`; concurrency lens still running — corrective held so one bounded round covers the union). Routing itself is CLEAN: every APIC row reaches the scheduler its contract specifies, no misrouted/collapsed/unimplemented row, no wrong-thread risk found. Binary compatibility intact (all 10 signatures exact, no `throws` change); tick semantics correct *for the right reason* — the reviewer extracted the real `IllegalArgumentException` thresholds from the pinned 26.1.2 build and confirmed the asymmetric floors match the platform rather than an off-by-one; the `cancel(int)` ABA attack fails; and the base `syncTasks`/`syncWhenFree` queues are never populated on Folia, so nothing is stranded behind the removed drain. **TWO ORCHESTRATOR ERRORS IN THIS TASK FILE, both found by the review and now corrected:** (1) the file cited an "architecture §3.5 C4 scheduler mapping table" **four times — no such table exists**; C4 is *Operation completion* (`architecture.md:187`). Same error class as FD-1. The worker independently detected it, substituted the frozen APIC-019–028 / APIC-092–101 rows, and RECORDED the substitution — correct behaviour, and the file now binds those rows explicitly. (2) **C6 was missing from the binding references**, which orphaned a real obligation: striking the global sync-drain tick removed its adaptive 5–50 ms TPS-aware time budget (`getAllocate()`, now dead code) and nothing replaced it, so every `sync` becomes an unbudgeted global task and they all run in one tick. C6 names `getAllocate()` explicitly, so this is task 16's; added to its scope with permission to escalate if the budget cannot be derived without task 14's slice controller. Other MAJORs for the corrective: `syncWhenFree` priority is a one-turn gate, not priority (ordering breaks under concurrent submission); `cancelAll()` cancels handles but never completes `awaitGlobal`'s future, stalling a worker on a 60 s owner-wait past shutdown while `DrainReport` reads 0 unresolved; `register()` → `reapFinishedTasks()` is an O(n) sweep per submission (up to 65 536 volatile reads, hit every 50 ms by `AsyncPreloader`) where Bukkit is O(1); and the "ten descriptors preserved" test is **vacuous** — it asserts only `getReturnType() == Future.class`, which cannot fail for any legal override, yet the dev record cites it as evidence for a claim that is true but unproven by it. **Concurrency lens = REJECT, 1 BLOCKING** (`review-w1-task16-concurrency.md`), verified against the actual PaperMC `FoliaGlobalRegionScheduler`/`FoliaAsyncScheduler` sources rather than from memory: at `FoliaTaskManager.java:610` the `finally` runs `complete()` when a **repeating** task's body throws — but Folia CATCHES, LOGS and RESCHEDULES, so the map entry is dropped and the permit released while the handle keeps firing. The task becomes permanently uncancellable and untracked: `cancel(id)` silently no-ops, `cancelAll()` cannot see it, 20 stack traces/second at `interval=1`. Orchestrator-confirmed blast radius: `Fawe.java:139`/`:141` register two server-lifetime repeating tasks at interval 1, plus `Regenerator.java:107` which holds the id for a later cancel. **The test certifies the bug** — it passes only because `FakeScheduledTask.fire` models Bukkit semantics instead of Folia's; a fake that disagrees with the platform invalidates everything built on it. Other MAJORs: `DispatchFuture` has no deadline where `awaitGlobal` has 60 s; `cancelAll()` never completes `awaitGlobal`'s future, so a worker stays parked past shutdown while `DrainReport` reads 0 unresolved; and nothing fails closed around the context predicate. **Orchestrator-verified gap surfaced by that last point: there is NO production Folia `FaweThreadContext` implementation in the tree at all** — only `BukkitThreadContext`, the core interface and two test fakes. Task 17's mandate says "select and register the Folia impl", which reads as though it exists; its file now states that task 17 must WRITE it too, with real ownership queries and never region-ID comparison. **Corrective 1 DISPATCHED** (`.codex/w1-task16-corrective1.*`, resumed thread `019f7eb4-79d8`) covering both reviews' union; the r12-added target-bearing routes are explicitly held out of this round so it stays bounded. History: DISPATCHED 2026-07-20 (fresh Codex thread, xhigh, `.codex/w1-task16.*`). Unblocked early: deps 10 (VERIFIED) + 12 (alignment VERIFIED today); scope is folia-module-only and explicitly excludes the `isMainThread` migration, so it is disjoint from 13 and 15. Concurrency mitigation issued: verify against the pre-built core classes at `worldedit-core/build/classes/java/main` (green as of today's checkpoint) instead of recompiling core sources, which task 15 is concurrently mutating |
| 11 corrective 4 | **DELIVERED + orchestrator-VERIFIED 2026-07-20, no escalation.** The r13 §3.6d contract is implemented on disk and I checked it rather than the report: `tryAcquirePlanProducer` (`:126`) and `tryAcquireAdmissionProducer` (`:164`) exist, the three types are declared (`PlanProducerToken` `:911`, `AdmissionProducer` `:1141`, `AdmissionDeliveryLease` `:1408`), the obsolete r10 shapes (`tryAcquireLease`, `claimRegisteredProducer`, lease-coupled `registerProducer`) are GONE, the token-consuming `DefaultOperationCompletion.register(PlanProducerToken)` overload is at `:80`, and **`OperationCompletion.closeAdmission()` is untouched** with the `// pending Amendment 3` markers still in place at two test sites. 57 focused tests, 20 consecutive runs. **The core seam is now stable at r13**, which is what unblocks tasks 13 and 14. |
| 13 fresh corrective 1 | **DELIVERED 2026-07-20, status BLOCKED on 2 contract gaps + 1 dependency.** Fixed: r13 producer binding, continuation/global accounting (the fail-open BLOCKING), nonblocking `tryAcquire`, producer-backed cancellation/deadlines/flush hooks, delivery isolation, waiter partitions, atomic telemetry. The 18-test suite now has real contention tests for lifecycle-lock fast misses, accounting-lock cancellation, grant/cancel races, common-pool starvation, flush-expiry batches, and byte/global caps — i.e. the two blind spots (zero byte-accounting assertions, zero global-cap coverage) are closed. Java 25 `-Xlint:all -Werror` passes. **Two genuine CONTRACT GAPS escalated, not improvised** → architecture round **r14 DISPATCHED** (`.codex/arch-cosign-r14.*`): (Q1) cancellation cannot participate in the attempt-level delivery CAS through the frozen API — `deliver` and `reject` are the only entries, and routing a user cancel through `reject` would terminalize the plan `NOT_ACCEPTED`, burning the plan sequence for an operation that may still be viable; (Q2) tick-thread `close()`/FINALIZING entry has no legal non-wait fallback, because the admission producer is released once the grant is published, long before the permit is later closed — so §3.6c item 4's "enqueue under the pre-acquired lease" has no live lease at that point. Third blocker (task 14 must supply value-equal `RegionKey`s) needs no ruling — already a HARD PRECONDITION in task 14's file. |
| r14 ruling | **RULED + APPLIED 2026-07-20** (`.codex/arch-cosign-r14.msg`; both clarifications, NO frozen SPI change). **Q1** → a THIRD outcome entry `AdmissionProducer.cancel(CancellationException)` competing on the same attempt-level CAS as `deliver`/`reject`, with the decisive semantic split: `reject` terminalizes the plan `NOT_ACCEPTED` and forces a new plan sequence, whereas **`cancel` completes the admission future exceptionally WITHOUT terminalizing the plan — the same plan sequence may retry**. So a player `//cancel` no longer burns a still-viable operation's plan sequence; if operation-level cancellation really does abandon the plan, task 14 terminalizes it `CANCELLED_BEFORE_MUTATION` explicitly rather than letting admission infer it. **Q2** → option (a), and it CORRECTS my own overbroad §3.6c item 4: after grant publication, `transfer`/`enter(FINALIZING)`/`close` are **pure synchronous accounting**, need no producer or lease, publish nothing, and newly-eligible waiter work goes through THAT waiter's own producer, never the released one. Item 4 additionally now forbids these tick paths from **waiting on `accountingLock`** at all — they need owner-local/atomic accounting or a bounded no-wait handoff, which is real work for task 13's next round. §3.6c item 4 and §3.6d rewritten accordingly. |
| r15 ruling | **RULED + APPLIED 2026-07-20** (`.codex/arch-cosign-r15.msg`; clarification, NO frozen SPI change — `DrainReport` unchanged). Chose **(d): incrementally maintained immutable snapshot** — the best available answer, because it refuses BOTH degradations: no exception is carved into the bounded-hook rule, and the report is not capped or truncated, so **G6 keeps exactly the evidence it has today**. The dispatcher maintains the snapshot as tasks/tickets register and retire (bounded per update, structural sharing, never rebuilding or sorting the label population on a tick thread); the expiry hook does only an idempotent waiter claim, ONE atomic snapshot read, and submission of the already-built immutable report. Rendering happens on the consumer. Counts stay exact and mutually coherent; label order need only be deterministic, not lexically sorted at expiry. General rule added: any producer needing an unbounded terminal diagnostic must maintain a publishable immutable snapshot incrementally or declare a capped contract in advance — **flush-expiry hooks never gain an implicit unbounded-snapshot exception**. Applied to §3.3 after the `DrainReport` record. |
| 12 alignment 3 | **DELIVERED + orchestrator-VERIFIED 2026-07-20, no escalation.** Persistent AVL label index gives the structural sharing r15 asked for — O(log n) per task update, O(1) per ticket update — and one atomic epoch carries the exact counts, the immutable label view, and a **prebuilt** `DrainReport`. Verified on disk: the expiry path is `diagnosticSnapshot.get().report`, a single read with no construction, traversal, copy or sort, and an instrumented `snapshotWorkProbe` FAILS the test if construction occurs during expiry — i.e. the O(1) claim is enforced by the suite rather than asserted in prose. Natural completion and expiry share one publication path, closing the divergence risk I added to the mandate. 17/17 tests across three runs. Residual style note for cross-review still stands: the publication barrier uses `synchronized (waiterReference)` on a local `AtomicReference`, and the expiry hook briefly waits on that monitor — bounded, but it is a wait inside a hook whose whole point is boundedness. History: DISPATCHED 2026-07-20 (`.codex/w1-task12-alignment3.*`, effort `high`) — implement the r15 snapshot. Scope also requires the NORMAL completion path to use the same snapshot as expiry, so the two report paths cannot diverge in exactly the evidence G6 depends on, and re-forbids the incoherent label/count snapshot that the first alignment's cross-review had already caught once. |
| 13 fresh corrective 3 | **DELIVERED + orchestrator-VERIFIED 2026-07-20, NO remaining escalation.** All four MAJORs closed, and the two that mattered are closed **structurally rather than by case analysis**, which is what I demanded: settled-batch publication is now guaranteed by construction (the `serviceWaiters` charge-then-submit-outside-`finally` window is gone), and idle-region pruning is bounded. **The cross-task MAJOR-4 is closed in the strongest possible way — by the type system.** `AdmissionRejectionHandler.failed(...)` now takes an `AdmissionFailureOutcome` (`CANCELLED` / `REJECTED`) parameter, so task 14 **cannot** wire the hook to the `NOT_ACCEPTED` sink blindly even though it was dispatched before my hard constraint was written into its file — the compiler forces the distinction. A note in a task file the worker may never re-read would not have achieved that. `transfer` now yields distinct outcomes (`TRANSFERRED` / `RETRY_BUSY` / `TARGET_SATURATED` / `PERMIT_CLOSED`) so a transient lock miss can no longer be read as "cannot admit, terminate without mutation". **Frozen-SPI discipline held under pressure:** `FoliaBackpressure.transfer` still returns `boolean` and `FoliaBackpressure.java` shows ZERO diff — the enum is exposed through an additional internal `transferResult(...)` seam beside the frozen override, rather than by relaxing the freeze. 29 tests, five consecutive runs, full Folia compilation, two independent PASS reviews. Expect a small task-14 alignment: it was dispatched against the pre-corrective-3 handler signature. |
| 11 corrective 6 | **DELIVERED + orchestrator-VERIFIED 2026-07-20, no escalation.** BLOCKING 2 is now protected by a **falsifiable** regression (`admissionProducerFencesTerminationAndClosedGuardSuppressesRedispatch`) that pins the mechanism which actually closes it — the admission lifecycle fence plus the closed guard — instead of the unreachable rollback. The unreachable rollback and the decorative guards are **removed**: a grep for `assert false` / "invariant failure" now returns nothing, so the third recurrence of the dead-guard pattern is closed at the root rather than papered over. Also added real admission flush re-clamping (closing the one-shot drain clamp gap) and strengthened the capability, sequencer, drain-expiry and `cancel` lock-discipline tests. 61 tests, 20 consecutive runs. Amendment 3 untouched. **Task 11 is now the most heavily assured component in the wave: 6 correctives, 3 independent adversarial review rounds (5 lenses), and its last verdict was PASS-WITH-NOTES with the residual notes now closed.** |
| 13 re-review c1+c2 | **PASS-WITH-NOTES** (`review-w1-task13-c1c2.md`). **All five prior BLOCKINGs confirmed genuinely closed** — the reviewer reconstructed each failure and every reproduction now fails, and resolved the decisive `deliverSettled` unlock→deliver→relock question by READING `OperationCompletionService` rather than trusting the code comment (closed by the single-thread executor + `serializationLock` FIFO ordering). Both r14 rulings conform clause by clause: exactly one `producer.cancel` site, release-before-cancel under the lock, and **no `accountingLock.lock()` anywhere on a permit-lifecycle path** across all 11 enumerated lock sites. Caps provably unbreachable: gated dimensions increment only under the lock while lock-free region paths only decrement, so a partial release reads as an UNDER-estimate of free capacity. **The no-wait property is proven by test, not prose** (a foreign thread parks the real lock and bounds the three transitions at 200 ms; a 100-iteration close/transfer race proves exactly-once release). **Four MAJORs → corrective 3 dispatched** (`.codex/w1-task13-fresh-corrective3.*`): (1) `serviceWaiters` charges up to 128 waiters then submits OUTSIDE any `finally` — BLOCKING-#1's shape relocated one frame up, held "by case analysis, not by construction"; (2) `pruneRequests`/`regions` grow unbounded when `tryLock` keeps missing — the named OOM vector; (3) `transfer` conflates "busy" with "target saturated", and since §3.7 rebinds every drain and an unsettled transfer terminates the plan WITHOUT mutation, a transient lock miss can cost a user their operation; (4) **URGENT and cross-task — the rejection hook fires indistinguishably for `cancel` and `reject`**, so if task 14 wires it to the `NOT_ACCEPTED` sink (the obvious reading, and task 14 is being written right now) a player `//cancel` burns a still-viable plan sequence behind the outcome CAS, re-opening exactly what r14 Q1 closed → recorded as a **HARD CONSTRAINT in task 14's file** as well. Three tests cannot fail on their named property, including `deadlineExpiresWhileCommonPoolIsSaturated` — `orTimeout` throws the same exception the assertion accepts, so it passes against an implementation with NO deadline mechanism, and it was the sole named evidence for a previously-BLOCKING item. No test injects a throw into delivery submission, so the BLOCKING-#1 fix has zero coverage. Dev-record escalation is stale: `RegionKey` already has value identity in tree. |
| 11 re-review c3+c4+c5 | **PASS-WITH-NOTES, NO BLOCKING** (`review-w1-task11-c3c4c5.md`) — the first non-red verdict of the session. Both corrective-3 BLOCKINGs confirmed CLOSED, §3.6d conforms clause by clause, and the corrective-5 three-outcome CAS tests were called **the strongest set in the file**: deterministic, genuinely falsifiable, non-union-safe race assertion with a deterministic tail, plus a real reflection-driven lock probe. **But the sharpest finding is epistemic, and it matters more than its MAJOR rating suggests:** BLOCKING 2 is closed **by a different mechanism than the dev record claims**. The `maybeComplete` rollback is safe but UNREACHABLE — `admissionDrainExpired` clears `registrationOpen` and the admission lifecycle producer fences `terminateIfQuiescent`, so `closeAdmission()` always returns at its guard once TERMINATED. Consequently the named regression test **cannot fail on its property**: delete the rollback and it stays green, so nothing protects the mechanism that actually closes the defect. Also MINOR-2: the "TERMINATED with a live token is an invariant failure" guard is **unreachable dead code** — the third recurrence of this pattern in this task, after I had explicitly warned about it; a guard that cannot fire advertises a safety net that does not exist. Plus a `…Locked` method called without its lock, a one-shot drain clamp with no `onFlushing` re-clamp, `cancel` missing from the lock-discipline probe, and three overstated dev-record claims. → **corrective 6 DISPATCHED** (`.codex/w1-task11-corrective6.*`, effort `high`), narrow: make the regression pin the REAL mechanism, resolve every guard that cannot fire (fix or replace with the structural argument — no decorative asserts), and correct the record. Reviewer caveat recorded: the task-11 files are untracked, so no baseline diff was possible; all conclusions came from reading the delivered state and hand-constructing interleavings. |
| 13 fresh corrective 2 | **DISPATCHED 2026-07-20** (`.codex/w1-task13-fresh-corrective2.*`, effort `high`) — the two r14 rulings. Carries the `reject` vs `cancel` call-site audit, and the substantive part: r14 Q2 now forbids `transfer`/`enter(FINALIZING)`/`close` from **waiting on `accountingLock`** at all, so those paths need owner-local/atomic accounting or a bounded no-wait handoff while preserving exactly-once release — with explicit permission to escalate if no-wait and exactly-once cannot both hold within the co-signed contracts. Also carries the newly-explicit rule that capacity released by permit A must wake waiter B through **B's own** producer, never through A's released one. |
| 11 corrective 5 | **DELIVERED + orchestrator-VERIFIED 2026-07-20, no escalation.** `AdmissionProducer.cancel(CancellationException)` is on disk (`:1271`) with the shared three-way outcome CAS and no plan terminalization; `OperationCompletion.closeAdmission()` still frozen, Amendment 3 untouched. Tests include a precisely pinned cancel-loser race after delivery and producer release, and a same-plan retry after cancellation. 60 tests, 20 consecutive runs. History: DISPATCHED 2026-07-20 (`.codex/w1-task11-corrective5.*`, effort `high`) — narrow: add `AdmissionProducer.cancel(...)` and its three-way-CAS tests. Sequenced BEFORE task 13's next round, which consumes it. |
| 12 alignment 2 | **DELIVERED 2026-07-20 with a well-founded `NEEDS_CONTEXT`.** Migrated to the r13 3-arg `registerProducer` with a `DrainProducerKey`, an atomic expiry hook registered at registration time (closing the window where a producer lacked a terminalizer), and a publication barrier; preserved the isolated stages, accounting-before-publication order, `failDrain`, and the r12 tick-thread hop; 14/14 tests. **Escalation is a real contract tension I had not seen:** r10 Q1 requires the flush-expiry hook to be BOUNDED and to perform no waiter-set scan, but producing a full `DrainReport` requires copying and sorting the outstanding-labels list — O(outstanding), unbounded by construction. So at drain-deadline expiry the hook must either break the bounded rule or publish something other than a full report. **This touches certification, not just style:** §3.6 makes non-zero unresolved tasks/live tickets in `DrainReport` a FAILED G6 gate, so degrading the report changes what the gate can observe precisely in the situation where the diagnostic matters most. → architecture round **r15 DISPATCHED** (`.codex/arch-cosign-r15.*`) with the four candidate shapes (permit the snapshot / degrade-with-truncation-marker / fail exceptionally / precomputed incremental snapshot) and an explicit demand to state what G6 observes under the chosen option. Note for the eventual cross-review: the new publication barrier uses `synchronized (waiterReference)` on a local `AtomicReference` — sound in shape and briefly held, but unusual style worth a reviewer's eye. History: DISPATCHED 2026-07-20 (`.codex/w1-task12-alignment2.*`, effort `high`). The sealed dispatcher drifted AGAIN: core now requires `registerProducer(producerKey, FlushExpiryHook, transitionFailure)` (3 args, `:104`) while the dispatcher still calls the 1-arg form (`:258`). **Not a pure signature swap** — the new middle argument is r10 Q1's drain-expiry terminalizer, so the waiter's existing `onFlushing` behaviour must move INTO the registration call, closing the window where a registered producer lacks a terminalizer. Worker also asked to re-check `submitCompletion` against r12 Q4, which since made the tick-thread rule explicit (fail before acquiring the primitive, never run the inline fallback). |
| 13 fresh corrective 1 dispatch note | **DISPATCHED 2026-07-20** (`.codex/w1-task13-fresh-corrective1.*`, resumed thread `019f7eb1`, **effort `high` per the new user directive**; named `-fresh-` because `.codex/w1-task13-corrective1.*` belongs to the RETIRED attempt and is historical evidence — do not overwrite it). Carries both reviews' union plus the r11/r13 RE-SCOPE: task 13 now owns only waiter-specific `AdmissionProducer`s, task 14 owns plan-producer preflight/registration and hands over a registered token via `bind(PlanProducerToken)`. The prompt opens by owning my incorrect salvage ruling, so the worker treats the salvaged accounting as unreviewed rather than trusted. |
| 15 corrective 1 | **DELIVERED + orchestrator-VERIFIED 2026-07-20 — status is deliberately BLOCKED ON 16/17, which is the correct outcome.** Census re-derived again under the widened r12 perimeter: **34 rows — 26 requalified, 2 retained, 6 WITHHELD**. The Paper recursion is genuinely gone: `createWorldNativeAccess` now returns the original two-argument `PaperweightFaweWorldNativeAccess(this, weakRef)` with no adaptation edge, and I confirmed only 3 files still differ from `HEAD` across adapter-26.1 + bukkit main. The 6 withheld sites are truly RESTORED to the legacy predicate (verified by grep: `LazyBaseEntity:27`, `NMSAdapter:133`, `PaperweightFaweWorldNativeAccess:102`/`:280`, `BukkitBlockCommandSender:194`, `FoliaTickThreadGuard:27`) rather than shipped inert — exactly what r12 Q1 ordered. **The guard was rebuilt better than I asked:** it moved from an UNTRACKED standalone Java file into the TRACKED `build-logic/src/main/kotlin/buildlogic.common.gradle.kts` convention, which dissolves the commit-split hazard and the cross-project/`configureondemand` conflict in one move, adds `outputs.file(stamp)` + `cacheIf` (closing the never-up-to-date finding) and an `inputs.property("c5GuardVersion")`, and now scans the full r12 perimeter including raw server-thread identity comparisons and method references. Crucially its exemptions are **exact-line, exact-count allowances**, not file exclusions — a new violation in an allowed file still fails. **NEW HAZARD I have locked down:** an allowance that outlives its reason silently converts "temporarily withheld" into "permanently legacy". Removing the corresponding `c5Allowances` entry is now written into the DEFINITION OF DONE of task 16 (4 sites) and task 17 (`FoliaTickThreadGuard`, plus filling the reserved-but-nonexistent `FoliaThreadContext.java` slot already listed in `c5ApprovedBackends`). |
| 16 corrective 1 | **DELIVERED + orchestrator-VERIFIED 2026-07-20.** BLOCKING closed at the root: `execute()` now releases the registration only `if (!repeating)`, so a repeating task that throws stays tracked and cancellable under Folia's catch-log-reschedule semantics. **The test fake was fixed first**, as instructed — it now catches the throwable and returns a repeating task to `IDLE` (reschedule) versus `FINISHED` for one-shot, i.e. it models the real platform instead of Bukkit. Also: bounded incremental reaping replaces the per-submit O(n) scan; queue deadlines added; `cancelAll()` wakes waiters immediately; fail-closed platform-context checks added; real cross-thread attach/cancel and claim/cancel race tests added; descriptor tests strengthened to detect inheritance/signature/`throws` drift (the vacuous return-type assertion is gone). `NEEDS_CONTEXT` escalated — and legitimately, since I authorized it: strict `syncWhenFree` priority and the C6 target-region budget both require **task 14's target-lane adaptive slice surface**. r12's deferred scope was read, not started, as instructed. |
| 14 commit broker | **DELIVERED 2026-07-20 — graph compile GREEN after it** (`/tmp/graph-compile-s4c.log`, exit 0). All five convergence obligations addressed: **`RegionKey` is now an immutable value type** (verified on disk: `final class`, `private final UUID worldId` + `long observedRegionId`, `equals`/`hashCode` over both) — the HARD PRECONDITION is satisfied, so task 13's per-region caps no longer risk degenerating into per-attempt caps; region lanes, adaptive time slices, DRR fairness, continuation re-entry, split/merge migration and transfer retry; plan-producer registration/cancellation/bounded expiry/rejection handoff; the task-16 target-lane sync-priority seams (unblocking task 16's escalated C6 budget and `syncWhenFree` priority); frozen regional/global diagnostics with the exact `FAWE_QUEUE` mapping. 14/14 broker tests over five runs, 29/29 backpressure regressions, full Folia compilation. The worker stated **what breaks each test** rather than that they pass — changed DRR weights/order, fixed-count or unmeasured slicing, stale-lane mutation, lost wakeups, cancellation forwarding, diagnostic mappings, and identity-based `RegionKey` accounting. **One `NEEDS_CONTEXT`, verified real before escalating it further:** `register(PlanProducerToken)` has FOUR reject paths (wrong service, wrong operation, admission closed, duplicate plan key), and §3.6d provides no abort — an acquired-but-unconsumable token leaves its producer and flush hook registered forever, so `terminateIfQuiescent` never sees `producers.isEmpty()` and **shutdown hangs**; meanwhile §3.6d calls "TERMINATED with a live token" an invariant failure, boxing the caller in. The worker correctly refused to paper over it by terminalizing the plan — a rejected registration is not a plan outcome. → architecture round **r16 DISPATCHED** (`.codex/arch-cosign-r16.*`), which also asks what happens to an ABANDONED token and whether `AdmissionProducer`/`arm(...)` has the same stranding shape. |
| r16 ruling | **RULED + APPLIED 2026-07-20** (clarification, NO frozen SPI change). Option (a) **with mandatory coordinator auto-abort**, which puts the burden in the right place: `PlanProducerToken.abort(Throwable)` is added, and **`register(token)` becomes TRANSACTIONAL** — every validation/insertion failure must call `token.abort(failure)` before rethrowing. So the four rejection paths clean up automatically and task 14 needs no exception-specific handling; it only has to abort if it elects not to register at all. Acquiring a token is a **linear obligation**. Abandonment is an invariant failure that must be **detectable** (unconsumed-token diagnostic) and **bounded** — at the drain deadline the expiry hook aborts it, so flush stays bounded instead of hanging. Explicitly: **no GC finalizer or `Cleaner` may provide correctness**. The rule deliberately does NOT transfer to admission producers: no `AdmissionProducer.abort(...)` is added; `arm(...) == false` means expiry already owns the attempt and its trampoline must reject the plan `NOT_ACCEPTED`, publish, and release; pre-`arm` `reject(...)` is explicitly legal and mandatory on setup failure; an abandoned unarmed producer is force-rejected at flush expiry. Applied to §3.6d. |
| r17 ruling | **RULED + APPLIED 2026-07-20** (all clarifications; NO frozen SPI and NO §4c supported-interface change — the 653 `PRESERVED` declarations are untouched). **Q1:** a dedicated `QueueHandlerRouting` facade in `QueueHandler`'s own package, `@ApiStatus.Internal`/`INTERNAL-NONAPI`, rather than widening `syncOn` or adding `TaskManager` entry points — callers still never name a Folia dispatcher (F1). **It also added a rule that overturns a design a reviewer had specifically praised: multi-target work MUST PARTITION by `ChunkTarget` and dispatch each partition separately — proving ownership of one, or even of ALL, targets does not authorize executing foreign-region work in one callback.** So task 15's conjunctive `ownsCachedChunks()` was a correct predicate but is not a sufficient routing strategy. **Q2:** thread an `EntityTarget` through an additive `LazyBaseEntity` constructor — **degradation was explicitly NOT authorized**; a non-owning tick caller fails before dispatch because the getter cannot await. **Q3:** `NMSAdapter` gains an async `setSectionAtomic(...)` doing exactly one CAS inside the facade under a fresh ticket, with **no non-owner branch and no FAWE-lock fallback** — the combination that made the previous overload gate nothing; the legacy String overload survives for excluded adapters behind `permitsLegacyLocationFreeLiveState()`. **Wave-1 exit: the co-signer REJECTED the deferral option I put on the table** — zero temporary C5 allowances may survive W1 exit in Folia-enabled source sets, and *"no worse than upstream is not Folia certification"*; deferral would need a signed degradation. Applied to §3.5. |
| 16 corrective 3 | **DELIVERED + orchestrator-VERIFIED 2026-07-20, graph compile GREEN** (`/tmp/graph-compile-s4f.log`, exit 0, adapter-26.1 included). All four r17 routes built: `LazyBaseEntity` entity-owner serialization, async ticketed `NMSAdapter.setSectionAtomic`, per-`ChunkTarget` cached-flush **partitioning** (the owner-context test proves three targets → three isolated callbacks under live tickets, so a single multi-chunk callback is impossible by construction), and command-block owner routing. **All five task-16 C5 allowances removed** — verified on disk that only the `Fawe.java` definition site and the task-17-owned `FoliaTickThreadGuard` remain, and that zero legacy/`isPrimaryThread` predicates survive in the enabled source sets. **Orchestrator fixed a build-logic compile error the worker could not catch** (Gradle is orchestrator-owned): corrective 3 added an AST-based guard check (`com.sun.source`) that referenced `SimpleJavaFileObject.Kind.SOURCE`, which Kotlin cannot resolve through the subclass — corrected to `JavaFileObject.Kind` + import. core 7/7, Bukkit 5/5, adapter 1/1, Folia 17/17. |
| 16 corrective 3 dispatch note | **DISPATCHED 2026-07-20** (`.codex/w1-task16-corrective3.*`, effort `high`) — build the facade, re-route all four sites under the partition rule, thread the entity target, add the routed section CAS, and **delete every unblocked `c5Allowances` entry** (now a hard W1-exit condition, not a cleanup). |
| 16 corrective 2 | **DELIVERED 2026-07-20 — C6 and strict priority DONE, four external sites escalated with a fully justified `NEEDS_CONTEXT`.** Delivered: all off-tick and target-bearing sync work now enters task 14's adaptive broker lanes; queued `NORMAL` strictly precedes `WHEN_FREE`; the C6 budget is proven by a test showing only 4 of 10 200 µs callbacks execute in the first 750 µs slice — a real measurement, not an assertion that something was scheduled. **The escalation is structural and I verified all three obstructions on disk before accepting it:** (1) the §3.5 context-carrying surface is `protected` (`QueueHandler.java:350/359/368/377`) and **all four sites r12 Q1 requires to use it live outside that class and package** — the mandated route is unreachable from every site that must take it; (2) `LazyBaseEntity.getNbt()` holds only a `Supplier`, with the entity captured inside the lambda, so there is genuinely no `EntityTarget` to pass; (3) `NMSAdapter`'s `World`-carrying overload no longer exists, because task 15's corrective correctly reverted that file (its two branches ran an identical live-section CAS, so the `ownsChunk` gated nothing). Approximating any of these would have violated r12 — the worker refused, which is right. → architecture round **r17 DISPATCHED** (`.codex/arch-cosign-r17.*`), which also asks the sequencing question directly: **may wave 1 close with these four sites still withheld** (legacy predicate + counted allowance, routes deferred to wave 2 where the chunk write path is built anyway), or must they land before the W1-exit gate? Both sides stated honestly — the sites are no worse than upstream today since they were never Folia-correct, but an allowance that outlives its reason becomes permanent while the build stays green. Five counted allowances remain; task 15 stays blocked. |
| 16 corrective 2 dispatch note | **DISPATCHED 2026-07-20** (`.codex/w1-task16-corrective2.*`, effort `high`) — the deferred r12 scope, now unblocked on both sides: task 15's corrective withheld the affected sites, and task 14 delivered the target-lane slice surface task 16 had escalated for. Carries the four target-bearing routes (`LazyBaseEntity`, the conjunctive multi-chunk WNA flush, the `NMSAdapter` World overload whose two branches currently run the identical CAS, and `BukkitBlockCommandSender`'s C1-banned `BukkitScheduler` fallback), the C6 per-region budget, strict `syncWhenFree` priority, and — as **definition of done** — deletion of each `c5Allowances` entry it unblocks. Scope explicitly widened to four files outside the folia module, with task 15 idle. |
| 11 corrective 7 | **DELIVERED + orchestrator-VERIFIED 2026-07-20, no escalation, graph compile GREEN after it** (`/tmp/graph-compile-s4d.log`). `PlanProducerToken.abort(Throwable)` is on disk (`:1010`) and `register(token)` calls `token.abort(failure)` on its rejection paths (`:144`) — the transactional cleanup r16 required. Also: bounded abandonment via the drain-expiry hook (no GC finalizer used as the mechanism), unconsumed-token diagnostics, and the unarmed-admission-expiry guarantee of `NOT_ACCEPTED` + exceptional publication + release **without misdiagnosing a legal pre-`arm` rejection**. Tests pin all four rejection paths, the abort/expiry race, bounded abandonment, consumed-token abort, and `arm == false`. 65 tests, 20 consecutive runs. Amendment 3 untouched. |
| 11 corrective 7 dispatch note | **DISPATCHED 2026-07-20** (`.codex/w1-task11-corrective7.*`, effort `high`) — implements r16, plus the standing no-guard-that-cannot-fire rule restated, since that pattern has recurred three times in this task. Prerequisite for the small task-13/task-14 alignments the ruling implies. |
| 15 closure | **DONE-PENDING-17 (2026-07-20).** Final census **34 rows — 31 requalified/target-routed, 2 retained (definition + backend), 1 pending task 17**. The worker re-verified all five formerly-withheld rows against the now-routed tree and found **no mismatch**: rows 16/27/31/32/33 all use exact `EntityTarget`/`ChunkTarget` routes through `QueueHandlerRouting` with ticket assertions — i.e. the routes r17 specified are the routes actually in the tree, confirmed independently rather than assumed from task 16's report. The ONLY residuals are the two legitimate ones (the `Fawe.java` definition, the `BukkitThreadContext` backend) plus the single task-17-owned pending item (`FoliaTickThreadGuard` allowance + the unfilled `FoliaThreadContext` slot). Task 15 closes the instant task 17 lands the Folia context. |
| 17 bootstrap wiring | **DELIVERED + graph compile GREEN (full, incl. adapter-26.1) — double-review DISPATCHED.** `FoliaThreadContext` is WRITTEN and — the highest-stakes check of the wave, verified on disk — its `ownsChunk`/`ownsEntity`/`isGlobalContext` are backed by **genuine live Folia queries** (`Bukkit.isOwnedByCurrentRegion(world, cx, cz)`, `isOwnedByCurrentRegion(entity)`, `isGlobalTickThread()`), NEVER region-ID comparison. Bootstrap: `onLoad` detect→certify→select→register-context, `onEnable` TaskManager→`Fawe.set`→lazy QueueHandler + Folia graph, shared completion service with the frozen `stop→drain→flush→executors` shutdown order. **The FINAL `FoliaTickThreadGuard` C5 allowance is removed** — verified on disk that `c5Allowances` now holds only the two legitimate entries (the `Fawe.java` definition and the `BukkitThreadContext` backend), so **zero temporary C5 allowances survive**, satisfying the r17 W1-exit condition. C5 guard ran on all four projects in the green build. Reviewer armed on: FoliaThreadContext non-owner correctness, the r12-Q2 contractual init ordering, F1 (no eager folia classload off-Folia), and fail-closed half-init. → `review-w1-task17.md`. |
| 17 re-review | **PASS-WITH-NOTES, NO BLOCKING/MAJOR** (`review-w1-task17.md`). **The wave's highest-stakes code is confirmed correct by a reviewer who checked the folia-api jar, not just the source:** `ownsChunk`→`isOwnedByCurrentRegion(world, chunkX, chunkZ)` passes true CHUNK coords (javadoc-confirmed) and a thread ticking R1 asked about R2's chunk gets `false` — a live scheduler query, no `RegionKey` anywhere in the predicate path. `ownsEntity`/`isTickThread`/`isGlobalContext`/`isFaweWorker` (marker + non-null extent) all match §3.1. Registration at `WorldEditPlugin.onLoad:142` strictly precedes the first predicate reader, triple-guarded; core has ZERO folia refs, the folia backend is a `ServiceLoader` supplier never evaluated on Paper; one shared `OperationCompletionService` with the frozen shutdown order. Four MINORs → corrective 1 dispatched (`.codex/w1-task17-corrective1.*`): (1) **the ownership test is partly vacuous** — `assertEquals(0, sameRegionHintReads)` on a decoy method nothing calls, the "counter nothing increments" anti-pattern again; being rebuilt to assert `ownsChunk` returns FALSE for a same-world different-chunk input, which is the property that actually matters; (2) predicates throw rather than return false on unloaded/retired inputs — a deliberate throw-vs-false contract decision now forced; (3/4) the fail-closed abort and the production init sequence are only partly pinned by deterministic tests, the rest a recorded harness obligation. `workerRoleRequiresTheExtentCarryingThreadMarker` was singled out as genuinely strong. |
| 12 alignments 2+3 re-review | **DISPATCHED 2026-07-20** (`review-w1-task12-align23.md`) — paying the review debt on the two never-audited alignment passes (r13 producer API + r15 drain snapshot). Armed on: the flush-expiry hook's actual boundedness at expiry, the unusual `synchronized(waiterReference)` publication barrier, snapshot coherence (a torn label/count read was caught once pre-alignment), natural-vs-expiry report parity, and whether the AVL index is truly balanced or can degrade to O(n) on a region thread. |
| 17 bootstrap wiring (dispatch note) | **DISPATCHED 2026-07-20 — LAST task of wave 1** (`.codex/w1-task17.*`, effort `high`, fresh workspace-write thread). All of 10-16 delivered; graph compile GREEN. Load-bearing addition from the task-16 review: **it must WRITE the Folia `FaweThreadContext`** (`FoliaThreadContext`, genuine live ownership queries, never region-ID) — it did not exist — not just register it. Plus the ServiceLoader/reflective discovery seam (F1, no eager module classload off-Folia), the r12-Q2 contractual init ordering with a test that fails if any predicate read precedes registration, the shared backend-owned `OperationCompletionService` with the frozen shutdown order, fail-closed hardening, and deletion of the FINAL `FoliaTickThreadGuard` C5 allowance. |
| 14 (historical) | **Task 14 was the convergence point and DELIVERED (see the '14 commit broker' row above):** (a) the `RegionKey` HARD PRECONDITION recorded in its file — value equality, immutability, stable hash across merge/split rebind, else every per-region cap in task 13 silently degenerates into a per-attempt cap; (b) the r11/r13 **plan-producer** side — `tryAcquirePlanProducer` → `register(token)` → `terminal`/`rejectAdmission`, plus the `bind(PlanProducerToken)` handoff to task 13 (§3.6d); (c) the **C6 target-region budget** and strict `syncWhenFree` priority escalated by task 16, both needing its adaptive slice surface; (d) the rejection-hook wiring constraint (must not block in the drain path); (e) the frozen G4 `DiagnosticSnapshot` producers. 17 last, after 10-16, and its file now records that it must WRITE the Folia `FaweThreadContext` (which does not exist in the tree) and not merely register it, plus the r12 Q2 bootstrap-ordering test. |

### Wave 2 — Chunk pipeline (after wave 1 verified)

GET snapshot path, SET commit path, finalizers (tiles/entities/POI/light/packets) on
adapter-26.1 per C1/C2; history write-task requalification; per-region backpressure live.

### Wave 3 — Operations surface

EditSession/extents/command dispatch requalification, clipboard, brushes, preloader,
session manager concurrency, deterministic tests (scheduler routing, failure injection,
persistence round-trips).

### Wave 4 — Features & degradations

//regen, snapshots, remaining inventory items per spike outcomes; degradations documented
and amended into the signed contract where required.

### Wave 5 — Certification & delivery

Full gate suite (Folia 26.1.2 per A2.1; reserved slot for Folia 26.2 if published and
amended in by then), regression matrix (Paper/Spigot), perf budget
verification (dual baselines), consumer-plugin API gate, FOLIA.md, port report.
